# Pin the uv version Nixpacks installs during its setup phase.
# Without this, Nixpacks generates `pip install uv==$NIXPACKS_UV_VERSION`
# with an empty variable (-> invalid `uv==`) and the build fails.
[variables]
NIXPACKS_UV_VERSION = "0.11.13"
